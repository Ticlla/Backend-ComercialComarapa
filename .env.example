# =============================================================================
# Environment Configuration Template
# =============================================================================
# Copy this file and rename based on your environment:
#   - .env.development  (local Docker PostgreSQL)
#   - .env.staging      (Supabase staging)
#   - .env.production   (Supabase production)
#
# Usage:
#   hatch run dev:start    # Uses .env.development
#   hatch run stage:start  # Uses .env.staging
#   hatch run prod:start   # Uses .env.production
# =============================================================================

# Application
APP_NAME=comercial-comarapa-api
APP_ENV=development          # development | staging | production
DEBUG=true                   # true | false
API_VERSION=v1

# Server
HOST=0.0.0.0
PORT=8000

# Database Mode
DATABASE_MODE=local          # local | supabase

# Local PostgreSQL (when DATABASE_MODE=local)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/comercial_comarapa

# Supabase (when DATABASE_MODE=supabase)
# Get these from: https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-role-key

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173