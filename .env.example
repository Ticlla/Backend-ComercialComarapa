# =============================================================================
# Environment Configuration Template
# =============================================================================
# Copy this file to .env.development, .env.staging, or .env.production
# and fill in your actual values. NEVER commit files with real secrets!
# =============================================================================

# Application
APP_NAME=comercial-comarapa-api
APP_ENV=development
DEBUG=true
API_VERSION=v1

# Server
HOST=0.0.0.0
PORT=8000

# Database Mode: "local" for Docker PostgreSQL, "supabase" for cloud
DATABASE_MODE=local

# Local PostgreSQL (Docker) - for DATABASE_MODE=local
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/comercial_comarapa

# Supabase (Cloud) - for DATABASE_MODE=supabase
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_KEY=your-supabase-anon-key

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:8000

# AI Configuration (Gemini) - Get key from https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-flash-latest
